image: jobbitz/goci:1.12.4

stages:
- test

variables:
  PRJ: /prj

before_script:
- mkdir $PRJ $PRJ/out && mv * $PRJ && cd $PRJ
- go mod download

linter:
  stage: test
  script: golangci-lint run

testing:
  stage: test
  script: go test -cover ./...
